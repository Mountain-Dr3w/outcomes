import { HeroComparison } from '@/components/hero-comparison';
export const metadata = { title:'Hero options', robots:{ index:false, follow:false } };
export default function Page() { return <HeroComparison />; }
