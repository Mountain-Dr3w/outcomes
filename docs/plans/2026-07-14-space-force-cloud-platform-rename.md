# Space Force Cloud Platform rename

Replace the legacy public project name everywhere the site can expose it, including the case title, copy, slug, metadata, alt text, artifact URLs, screenshots, and downloadable deck. Describe it as an anonymized USSF cloud platform, never the singular official platform, and omit the client-company outbound link from the case page. Done when the production build and public files contain no case-insensitive match for the legacy name.
